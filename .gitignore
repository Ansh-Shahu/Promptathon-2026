# ── Node / Frontend ───────────────────────────────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/
frontend/.vite/
frontend/*.local
node_modules/
dist/
dist-ssr/
.vite/
*.local

# ── Python / Backend & ML Pipeline ────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.pyo
.Python
env/
venv/
.venv/
ENV/
pip-selfcheck.json
backend/venv/
backend/__pycache__/
backend/.env.local
backend/.env.*.local
ml_pipeline/venv/
ml_pipeline/__pycache__/
ml_pipeline/.env.local
ml_pipeline/.env.*.local

# Virtual environments
.env.local
.env.*.local

# Python packaging
*.egg-info/
*.egg
.eggs/
build/
develop-eggs/
dist/
downloads/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.dist-info/

# ── Database ──────────────────────────────────────────────────────────────────
*.db
*.sqlite
*.sqlite3
backend/*.db
backend/*.sqlite
backend/*.sqlite3
ml_pipeline/*.db
ml_pipeline/*.sqlite
ml_pipeline/*.sqlite3

# ── ML Artifacts (large binaries — uncomment to ignore) ───────────────────────
# backend/model.pkl
# ml_pipeline/model.pkl
# *.pkl
# Comment out above lines if you want to track trained model artifacts

# ── Large Data Files ──────────────────────────────────────────────────────────
*.csv
ml_pipeline/**/*.csv
# Uncomment above if CSV data files should not be tracked

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Environment / Secrets ─────────────────────────────────────────────────────
# NOTE: backend/.env is intentionally tracked so teammates get the default config.
# If you add real secrets, move them to backend/.env.local and add it here:
.env.local
.env.*.local
*.secret

# ── IDE / Editor ──────────────────────────────────────────────────────────────
.vscode/
.vscode-server/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.project
.classpath
.settings/
*.iml

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# ── Testing / Coverage ────────────────────────────────────────────────────────
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/
coverage.xml
*.cover

# ── Temporary / Misc ──────────────────────────────────────────────────────────
*.bak
*.tmp
*.swp
.cache/
