# This repository has folder-specific dependency management.
#
# Install dependencies for each section separately:
#
# Frontend:
#   cd frontend && npm install
#
# Backend:
#   cd backend && pip install -r requirements.txt
#
# ML Pipeline:
#   cd ml_pipeline && pip install -r requirements.txt
#
# For development, ensure Python venv is activated before installing backend/ml_pipeline deps.

